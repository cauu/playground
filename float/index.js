import FloatMath from 'float-math-util';

/**@desc 
 * 实现思路:
 *  首先将小数乘以一个倍数，都转换成整数，
 *  
*/
console.log(FloatMath);

function test() {
  console.log(Array.prototype.slice.call(arguments));
}

test(1, 2, 3);